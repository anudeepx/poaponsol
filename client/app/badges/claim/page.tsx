"use client";

import ClaimBadgeModal from '@/components/claim-form/ClaimBadgeModal';

const Page = () => {

  return (
    <ClaimBadgeModal open={true} onClose={() => {}} />
  );
};

export default Page;